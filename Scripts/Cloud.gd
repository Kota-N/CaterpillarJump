extends Sprite



func _ready():
	var animation = $AnimationPlayer
	animation.current_animation = "cloud"


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
